<%= form_with(model: doctor_patient) do |form| %>
  <% if doctor_patient.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(doctor_patient.errors.count, "error") %> prohibited this doctor_patient from being saved:</h2>

      <ul>
        <% doctor_patient.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :doctor_id, style: "display: block" %>
    <%= form.text_field :doctor_id %>
  </div>

  <div>
    <%= form.label :patient_id, style: "display: block" %>
    <%= form.text_field :patient_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
