<%= form_with(model: clinic) do |form| %>
  <% if clinic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(clinic.errors.count, "error") %> prohibited this clinic from being saved:</h2>

      <ul>
        <% clinic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :owner, style: "display: block" %>
    <%= form.text_field :owner %>
  </div>

<div>
  <% if clinic.avatar.attached? %>
      <%= image_tag clinic.avatar, style: "width: 200px; display:block" %>
    <% end %>
  <%= form.label :avatar, style: "display: block", class: 'field' %>
  <div class="control">
    <%= form.file_field :avatar, class: 'input', id: 'image' %>
  </div>
</div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
